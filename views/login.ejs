<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Register</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  </head>
  <body>
    <div
      class="w-full h-screen bg-zinc-900 flex justify-center items-center flex flex-col gap-4"
    >
      <h2 class="tracking-tighter text-white text-2xl">Login into Account</h2>
      <form class="flex flex-col gap-4" action="/login" method="post">
        <input
          class="px-6 py-2 rounded-md bg-zinc-800 text-white"
          type="email"
          name="email"
          placeholder="email"
        />
        <input
          class="px-6 py-2 rounded-md bg-zinc-800 text-white"
          type="password"
          name="password"
          placeholder="password"
        />
        <input
          class="bg-blue-500 py-2 text-white rounded-lg"
          type="submit"
          value="Login user"
        />
      </form>
    </div>
  </body>
</html>
